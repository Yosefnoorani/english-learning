# Gemini API key — ONLY for generating new words/sentences (Add Content button)
# Mistake feedback comes from public/feedback.json (no API needed)
# Get a key at: https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=your_api_key_here

# Optional: Supabase (leave unset for local-only mode)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
